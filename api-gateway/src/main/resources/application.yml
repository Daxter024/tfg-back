spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      routes:
        - id: season-service
          uri: ${SEASON_SERVICE_URL:http://localhost:8084}
          predicates:
            - Path=/api/season/**
          filters:
            - StripPrefix=1
            - name: CircuitBreaker
              args:
                name: seasonService
                fallbackUri: forward:/fallback/season
                statusCodes: 500,502,503,504,505
        - id: crop-service
          uri: ${CROP_SERVICE_URL:http://localhost:8081}
          predicates:
            - Path=/api/crop/**
          filters:
            - StripPrefix=1
            - name: CircuitBreaker
              args:
                name: cropService
                fallbackUri: forward:/fallback/crop
                statusCodes: 500,502,503,504,505
        - id: terrain-service
          uri: ${TERRAIN_SERVICE_URL:http://localhost:8083}
          predicates:
            - Path=/api/terrain/**
          filters:
            - StripPrefix=1
            - name: CircuitBreaker
              args:
                name: terrainService
                fallbackUri: forward:/fallback/terrain
                statusCodes: 500,502,503,504,505
# For newer versions
#  cloud:
#    gateway:
#      server:
#        webmvc:
#          routes:
#            - id: season-service
#              uri: http://localhost:8084
#              predicates:
#                - Path=/api/season/**, /api/season
#              filters:
#                - StripPrefix=1

logging:
  level:
    org.springframework.cloud.gateway: DEBUG
    org.springframework.web.reactive: DEBUG
    org.springframework.context.annotation: DEBUG
    reactor.netty: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: gateway, health, info

server:
  port: ${SERVER_PORT:9000}